#include<stdio.h>
int main()
{
    // based on the some condition when we need to use to stop iteration the we use branching statement(break)
    // we use break to stop further execution.
    // we can only use break loop and switch cases

    // for (int i = 1; i <= 10; i++)
    // {
    //     printf("hello world\n");

    //     if(i==5)break;
    // }
    // printf("rest of code\n");
    



    // based on the condition it skip the iteration so for that we use continue statement.

    // for (int i = 1; i <= 10; i++)
    // {
    //     if (i==5||i==3)continue;
    //     printf("%d\n",i);
    // }
    // printf("rest of the code\n");




    //we use goto keyword to excute the code without using loop

    // int i =1;
    // lable:  
    // printf("hello world\n");
    // i++;
    // if(i<=5)goto lable;


    

    









    return 0;
}
